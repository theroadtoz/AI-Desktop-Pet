# Phase 0 P0-1B 验收记录

## 执行日期

2026-06-17

## 执行命令

```powershell
git status -sb
npm.cmd run verify
npm.cmd run dev
```

## 自动验证结果

- P0-1A 本地提交已推送到 `origin/main`。
- 推送后本地状态为 `main...origin/main`，没有 ahead。
- `npm.cmd run verify` 已通过。
- `魔女/` 模型目录未纳入本次变更。
- 本地 Windows 自动化助手初始化失败，后续使用真实 Win32 桌面事件和截图完成人工交互验收。

## 人工桌面验收结果

### 人工验收日期

2026-06-17

### 通过项

- 日期：2026-06-17
- 透明背景：通过。
- 无黑底：通过。
- 置顶但不抢焦点：通过。
- 空白区域点击穿透：通过。
- 连续 10 次空白点击：通过。
- 点击头部打开聊天：通过。
- 点击身体打开聊天：通过。
- 空白区不误触发聊天：通过。
- 聊天窗口显示：通过。
- 聊天输入框聚焦：通过。
- 文字输入：通过。
- 角色拖拽：通过。
- 小幅点击不误判拖拽：通过。
- 连续拖动 10 次无卡死：通过。
- 拖拽释放后恢复穿透：通过。
- 连续角色点击 10 次：通过。
- 应用无崩溃：通过。

### 验收记录

- pet 窗口显示为约 420x600 的半透明 WebGL 占位角色，桌面截图确认透明区域能显示底层窗口，没有黑色矩形底。
- pet 窗口扩展样式包含置顶与 no-activate，空白区域点击后前台窗口保持为底层测试窗口，pet 不抢键盘焦点。
- 使用本地临时点击计数窗口放置在 pet 下方，空白透明区域 10 次点击全部落到底层窗口，且未打开聊天窗口。
- 头部和身体矩形命中区点击均能打开 `Desktop Pet Chat`。
- 聊天窗口打开后，输入框可聚焦，并可输入 `hellotest` 文本；当前阶段未验证真实 AI 回复。
- 小幅移动小于 4 DIP 时 pet 位置不变，未误判为拖拽。
- 角色区域拖拽后 pet 位置发生变化，连续 10 次拖拽无卡死。
- 拖拽释放后，透明空白区域恢复穿透；1 次和连续 10 次空白点击均落到底层测试窗口。
- 验收中发现：仅依赖 renderer `pointermove` 时，穿透状态下角色命中区无法可靠切回交互。已在主进程 pointer controller 中补充光标位置轮询，同步同一组矩形 hitAreas 后复测通过。
- 临时桌面截图和点击测试窗口已清理。

## P0-1B 实现项

- pet IPC 合同新增明确方法：`pet:pointer-hit-change`、`pet:open-chat`、`pet:drag-start`、`pet:drag-move`、`pet:drag-end`。
- pet preload 只通过 typed `PetApi` 暴露明确方法，没有通用任意 channel 入口。
- 主进程新增 pointer controller，集中管理点击穿透、60ms 延迟恢复和拖拽期间不穿透。
- renderer 使用矩形命中区：
  - head：x 25% - 75%，y 5% - 33%
  - body：x 22% - 78%，y 28% - 83%
- renderer 只在命中状态变化时通知主进程，避免每帧 IPC。
- 命中区内点击触发聊天窗口打开。
- 命中区内按下并移动超过 4 DIP 后进入拖拽，由主进程移动窗口。

## 待人工桌面验收

无。

## 已知问题

- 未接 Live2D。
- 未接真实 AI。
- 当前命中区为矩形近似，不做像素级检测。

## 是否允许进入 P0-2A

是。

P0-1B 人工桌面交互验收通过，允许进入 P0-2A。
